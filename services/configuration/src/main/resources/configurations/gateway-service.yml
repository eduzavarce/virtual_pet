spring:
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: "auth-service"
              uri: lb://auth-service
              predicates:
                - Path=/auth/**
              filters:
                - PrefixPath=/api/v1
server:
  port: ${GATEWAY_SERVER_PORT:8080}
